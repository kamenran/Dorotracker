import { generateSchedule, reschedule } from "../../../shared/scheduler.js";

export function schedulerFeaturePlaceholder() {
  return {
    feature: "scheduler",
    status: "working-demo-ready",
  };
}

export { generateSchedule, reschedule };
