# DoroTracker

Fresh project reset for the team build.

## Team feature split

- `nathan-feature`: authentication and accounts
- `kelsey-feature`: assignment CRUD
- `kamran-feature`: scheduler and rescheduler
- `beatrice-feature`: Pomodoro timer and progress UI

## Current base

This branch now contains only a clean starter structure:

- `frontend/` for the web UI
- `server/` for backend placeholders
- `shared/` for shared data contracts

Nothing opinionated or overbuilt is left from the earlier prototype.
